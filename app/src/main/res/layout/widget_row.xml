<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"  android:id="@+id/widget_row"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/padding_medium"
    android:paddingTop="@dimen/padding_medium"

    >

    <TextView
        android:id="@+id/TitleTextView"
        android:layout_width="100dp"
        android:layout_height="fill_parent"
        android:layout_alignParentLeft="true"
        android:text="testiong"
        android:ellipsize="marquee"
        android:singleLine="true"
        android:paddingLeft="@dimen/padding_small"
        android:textSize="17sp" />



    <TextView
        android:id="@+id/totalTextView"
        android:layout_width="50dp"
        android:layout_height="fill_parent"
        android:layout_alignParentRight="true"
        android:paddingRight="@dimen/padding_medium"
        android:text="testiong"
        android:textSize="17sp" />

    <TextView
        android:id="@+id/unackedTextView"
        android:layout_width="50dp"
        android:layout_height="fill_parent"
        android:layout_toLeftOf="@id/totalTextView"
        android:paddingRight="@dimen/padding_medium"
        android:text="testiong"
        android:textSize="17sp" />


    <TextView
        android:id="@+id/readyTextView"
        android:layout_width="50dp"
        android:layout_height="fill_parent"
        android:layout_toLeftOf="@id/unackedTextView"
        android:paddingRight="@dimen/padding_medium"
        android:text="testiong"
        android:textSize="17sp" />




</RelativeLayout>